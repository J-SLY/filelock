[package]
name = "filelock"
version = "1.2.1"
edition = "2024"
description = "XOR 加密/解密文件的 CLI 工具，支持基于偏移量的密钥生成与文件保护"
license = "MIT"
repository = "https://github.com/J-SLY/filelock"

[dependencies]
clap = { version = "4.5", features = ["derive"] }